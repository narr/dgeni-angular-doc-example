language: node_js

node_js:
  - "4.2"

install:
  - npm install -g grunt-cli
  - npm install -g bower
  - cd dgeni_ng_doc
  - npm install

before_script:
  - grunt build

script:
  - grunt

notifications:
  emails:
    - nardgu@gmail.com
